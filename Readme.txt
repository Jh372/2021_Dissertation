Folder Structure is as follows when running the following program.

CS5098
      |
      |-----CreateTrainingDataset.py(ipynb)
      |-----FFT.py(ipynb)
      |-----SVM.py(ipynb)
      |-----step2-----output5_csv(Folder) ------Data*.csv
      |-----image_file(Folder) ----- hieroglyph text(png file,after coverting format)
      |-----pdf_file(Folder) ---- hieroglyph text(pdf file)
      |-----test(Folder) ----crop----img(before merging)          
      |          |----------------output----- img(after merging)

Python3 files: Run from #1 to #3 in order
1.CreateTrainingDataset.py
2. FFT.py
3.SVM.py
(Jupyter Notebook:
CreateTrainingDataset.ipynb, FFT.ipynb, SVM.ipynb)
